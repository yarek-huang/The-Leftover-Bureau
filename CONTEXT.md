# CONTEXT

本文件是领域词汇表（glossary），只收录领域概念与术语，不收录实现细节。

## 冰箱与共享

- **Fridge（冰箱）**：食材存放与家庭共享的核心单元。一个用户可拥有/加入多个冰箱。只能归档（冻结只读、可逆，Owner 可恢复），不能真删除。
- **Membership（成员关系）**：用户与冰箱的从属关系。角色两级：
  - **Owner（主理人）**：建冰箱的人。管理成员、生成/撤销邀请码、归档/恢复冰箱。
  - **Member（成员）**：冰箱内平权使用者。增删改食材、发起推荐、记录卷宗。
  - 每冰箱成员上限 10 人。
- **InviteCode（邀请码）**：Owner 生成的 6 位加入凭证。一码多人共用，带有效期，可撤销。
- **Aggregated View（聚合视图）**：一人多冰箱时的默认视图——汇总展示全部活跃冰箱的食材，可按单冰箱筛选。推荐范围跟随筛选：单冰箱按单冰箱推，"全部"时跨冰箱合推。
- **Archive（归档）**：冰箱的冻结态：只读、保留全部历史、可逆。归档冰箱不进聚合视图与推荐范围。

## 账号与会话

- **User（用户）**：平台账号主体。食谱属于用户而非冰箱。注册为邀请制（需注册邀请码）。
- **Admin（管理员）**：可转让、可任命多个；首个注册用户自动为 admin；可重置任意用户密码、审核食谱。
- **Registration InviteCode（注册邀请码）**：现有用户生成，区别于 Fridge InviteCode。
- **OAuth Provider（社交登录）**：预留接口位（微信等），V1 不实现，公网迁移后接入。
- **JWT Session**：无状态会话，允许多端同时在线、互不踢。

## 食材库存

- **StockItem（库存条目）**：冰箱里的一条食材记录，单层实体、自带自由文本名（"番茄""半份红烧肉"），无字典层。字段：名字 / 数量（自由文本）/ 位置 / 状态 / 到期日（可跳过）。
- **StorageZone（位置三区）**：冷冻 / 冷藏 / 常温。录入必选，默认冷藏。
- **ItemState（状态）**：生鲜 / 熟食 / 剩菜。熟食剩菜保质期短，推荐优先级高。
- **红色通缉令（Expiry Alert）**：3 天内到期的条目高亮排序；过期未删持续高亮。
- **Fridge History（冰箱操作历史）**：条目级留痕——删除/消耗均记录（谁、何时、对哪条、动作）。

## 用户与食谱

- **User（用户）**：平台账号主体。食谱属于用户而非冰箱。
- **Recipe（食谱）**：用户私有 / 平台公开两级。精简字段：名称 / 食材清单 / 步骤 / 总耗时 / 人数份量 / 难度（易/中/难）。不建标签体系。
- **Recipe Ingredient Line（食谱食材行）**：自由文本名 + 自由文本用量 + **主料/调料标记**。调料/基础料默认家家都有，匹配不阻碍；只对主料要求在库（差一样仍推+补购单）。
- **荤素派生字段**：食谱入库时 AI 顺手标荤/素（派生、可改），仅供套餐搭配用。
- **MealSet（套餐推荐）**：一次给 2-4 菜搭配方案（荤素搭配），非单菜。
- **Taste Signal（口味信号）**：卷宗历史（做过什么+评价）作推荐偏好：常做加权、差评降权。
- **Leftover Rework（剩菜改造）**：剩菜条目优先"翻热即可/改造重做"（如剩红烧肉→烩面），走 LLM 现场生成存私有，不硬套生鲜食谱。

## 食谱审核

- **RecipeStatus（食谱状态）**：private → pending → approved（全平台公开）/ rejected（带拒绝理由，可改后重提 → pending）。
- **Review Queue（待审队列）**：用户提交的食谱进 pending，等 admin 审核。
- **Admin Direct-Create**：admin 直接创建的食谱跳过队列，直接 approved。

## 推荐引擎

- **Recommendation（推荐）**：两模式——极速清场（清冰箱，库内检索优先、库空降级 LLM 生成）/ 饕餮盛宴（高配，库内检索不够再 LLM 生成）。骨架 = 规则打分器产候选集 + LLM 语义匹配+组套餐+剩菜改造。**LLM 硬依赖**（自由文本名语义匹配离不开 LLM），LLM 不可用直接报错不降级。极速清场同步、饕餮盛宴异步。
- **Fridge Signature（食材签名）**：冰箱当前食材集合的指纹，作 LLM 响应缓存键；食材变动即失效。
- **Adjustable Weight（可调权重）**：临期加权与口味信号权重用户可调，默认临期命中必进候选、口味信号作同分决胜。

## 录入

- **Entry Channel（录入通道）**：V1 = 文字（批量多行表单）+ 拍照（拍→识别→逐条人工确认入库，识别结果绝不直接入库，失败退回文字表单预填）。语音延后至 V1 后。移动端两通道均响应式，拍照为主入口。
