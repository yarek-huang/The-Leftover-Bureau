# 推荐引擎设计：双模式推荐的工程方案

- Labels: wayfinder:grilling
- Status: closed
- Assignee: Yk
- Blocked-by: 5, 7
- Parent: 1

## Question

005 定了推荐的领域语义，007 给了 provider 事实，这张票定**工程方案**：

- 匹配算法骨架：食材集合 → 候选食谱的检索/打分（规则打分器？embedding 相似度？纯 LLM 判断？），临期加权、口味信号（卷宗历史）怎么进打分函数。
- 极速清场 vs 饕餮盛宴两条 pipeline 的共性与分叉点；"缺一两样也算匹配"的容忍度参数化。
- LLM 生成食谱（盛宴模式）的落地细节：prompt 结构、输出 schema（对齐 005 的 Recipe 模型）、校验重试、生成结果是否自动入库。
- 成本与延迟控制：一次推荐请求的 LLM 调用次数上限、缓存策略（Redis 还是不缓存）、失败降级（LLM 不可用时退化为纯规则匹配？）。
- 接口形态：推荐是同步请求还是异步任务（生成盛宴可能要几十秒）？

HITL：与 Yk 对谈定案，产出并入设计文档的推荐引擎章节。
